import 'package:components/config/size.dart';
import 'package:flutter/material.dart';

class Space {
  static Widget get x1 => SizedBox(width: 1.w);
  static Widget get y1 => SizedBox(height: 1.w);
  static Widget get x2 => SizedBox(width: 2.w);
  static Widget get y2 => SizedBox(height: 2.w);
  static Widget get x3 => SizedBox(width: 3.w);
  static Widget get y3 => SizedBox(height: 3.w);
  static Widget get x5 => SizedBox(width: 5.w);
  static Widget get y5 => SizedBox(height: 5.w);
  //

  //

  //
  // static Widget get top => SizedBox(height: 5.w);
  // static Widget get bottom => SizedBox(height: 5.w);
  // //
  // static Widget get xm =>
  //     const Expanded(child: SizedBox(width: double.infinity));
  // static Widget get ym =>
  //     const Expanded(child: SizedBox(height: double.infinity));
  //

  // static get h => EdgeInsets.symmetric(horizontal: 5.w);
  // static get v => EdgeInsets.symmetric(vertical: 5.w);
  // static get h1 => EdgeInsets.symmetric(horizontal: 5.w);
  // static get v1 => EdgeInsets.symmetric(vertical: 5.w);
  // static get h2 => EdgeInsets.symmetric(horizontal: 5.w);
  // static get v2 => EdgeInsets.symmetric(vertical: 5.w);
  // static EdgeInsets get z => EdgeInsets.zero;

  // static get hv => EdgeInsets.symmetric(vertical: 5.w, horizontal: 5.w);
  // static get hv1 => EdgeInsets.symmetric(vertical: 5.w, horizontal: 5.w);
  // static get hv3 => EdgeInsets.symmetric(vertical: 5.w, horizontal: 5.w);

  // //

  // static Widget xf(double no) => SizedBox(width: no.w);
  // static Widget yf(double no) => SizedBox(height: no.w);
  // //
  // static EdgeInsets t([double? no]) => EdgeInsets.only(top: no ?? 0.5.h);
  // static EdgeInsets b([double? no]) => EdgeInsets.only(bottom: no ?? 0.5.h);
  // static EdgeInsets l([double? no]) => EdgeInsets.only(left: no ?? 0.5.w);
  // static EdgeInsets r([double? no]) => EdgeInsets.only(right: no ?? 0.5.w);

  // static EdgeInsets hf(double no) => EdgeInsets.symmetric(horizontal: no.w);
  // static EdgeInsets vf(double no) => EdgeInsets.symmetric(vertical: no.w);

  // static EdgeInsets all([double h = 0.5, double? v]) =>
  //     EdgeInsets.symmetric(vertical: (v ?? h), horizontal: (h));
}
